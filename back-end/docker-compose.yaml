services:
  postgresql-test:
    image: postgres:16
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=teste
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
